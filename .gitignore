# Build artifacts — binaries and objects are reproducible from source
zero/day1
zero/day2
zero/*.o
zero/probe_*
*.out
a.out

# Disposable helpers
_probe_*.sh

# Secrets — never commit credentials of any kind
.env
.env.*
*.env
*token*
*secret*
*credential*
credentials
credentials.*
*.key
*.pem
*.pfx
id_rsa
id_ed25519
*.p12
.netrc
.git-credentials
gh-token.txt
