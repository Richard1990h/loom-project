# Build artifacts — binaries and objects are reproducible from source
zero/day1
zero/day2
zero/day3
zero/day4
zero/*.o
*.o
*.out
a.out
# hand-written probe executables (source .c/.cu is tracked; binaries are not)
probes/ram_triad
probes/cpu_flops
probes/gpu_devquery
probes/gpu_bandwidth
probes/gpu_matmul
probes/gpu_matmul_fast
probes/gpu_kernels
probes/gpu_attention

# Corpus bytes — gigabytes not tracked; data/manifest.json IS tracked
data/raw/
data/clean/

# Disposable helpers
_probe_*.sh

# Secrets — never commit credentials of any kind
.env
.env.*
*.env
*token*
*key*
*secret*
*credential*
credentials
credentials.*
*.key
*.pem
*.pfx
id_rsa
id_ed25519
*.p12
.netrc
.git-credentials
gh-token.txt
