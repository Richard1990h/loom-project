# archive-track1-torch (RETIRED)

RETIRED before ever executing on the desktop; sandbox-era measurements in
RESULTS.md and *.json remain valid historical records; no new results will
come from this code.

This directory is the former `loom/` PyTorch prototype (Track 1). Per the
permanent PURITY DECISION (see CLAUDE.md and ZERO.md), PyTorch and all ML /
numerics libraries are forbidden on this machine. Nothing here is run.
